Входные параметры: количество нитей,
затем для каждой нити количество циклов, которое она доджна поработать
и приоритет(чем меньше, тем быстрее работает).
зы. Вторая нить может завершиться только после первой и так далее.(Завершение в порядке запуска). 
