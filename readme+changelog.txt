Изменения с прошлой проверки:
0 задача: разбил на директории, также разбил readme на отдельный для каждой задачи(старая версия по преднему лежит в корне, если вам так удобней);
1 задача: добавил комментарии и обработку ошибок при работе с файлами;
2 задача: добавил комментарии(надеюсь так будет понятней зачем там 2 почти одинаковых цикла), убрал ограничение на 300мб и использовал, как вы и советовали конструкцию eval( ); так же обнаружил некоторый баг, в связи с чем были изменены регвыры в if'ах внутри циклов.
3 задача: без изменений
4 задача: добавил обработку ошибок при вызове pthread_join; добавил "cc" в блок GCC IA. с учетом того что "=a"(ret_val) говорит, что надо возвращать значение через регистр eax, добавление "%eax" в последний блок запрещается самим GCC.(ошибка в ходе компиляции).
5 задача: в разработке.
зы. будьте здоровы.   